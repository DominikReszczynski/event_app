import 'package:flutter/material.dart';

class SlaskieView extends StatelessWidget {
  const SlaskieView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
